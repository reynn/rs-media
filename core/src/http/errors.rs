//! RS Media Core HTTP Errors

// use {client, server};

error_chain! {
    // links {
    //     /// An internal error within the HTTP Client
    //     HTTPClientError(http::client::errors::Error, http::client::errors::ErrorKind);
    //     /// An internal error within the HTTP Server
    //     HTTPServerError(http::server::errors::Error, http::server::errors::ErrorKind);
    // }
}
